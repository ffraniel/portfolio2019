const WhyThisProcess = () => (
  <section className="why-this-process">
    <h2>Why this process?</h2>
    <p>I'd like to be there, it's real good, and hopefully something good comes of it.</p>
    <style jsx>{`
      .why-this-process {
        text-align: center;
        padding: var(--section-padding);
        color: var(--alt-text-colour);
        background-color: var(--alt-background-colour);
        font-family: var(--main-font);
      }
    `}</style>
  </section>
);

export default WhyThisProcess;